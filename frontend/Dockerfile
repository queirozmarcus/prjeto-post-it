# Stage 1: Build
FROM node:20-slim AS build
WORKDIR /app

COPY package*.json ./
# Force a clean install and upgrade
RUN npm install

COPY . .
# Run build, ensuring vue-tsc is used correctly
RUN npm run build

# Stage 2: Production
FROM nginx:stable-alpine
COPY --from=build /app/dist /usr/share/nginx/html

# Custom nginx config for proxy
COPY nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
